[package]
name = "sdl"
version = "0.1.0"
edition = "2024"

[features]
none = []
vulkan = ["gpu-vulkan"]

[dependencies]
interfaces.workspace = true
gpu-vulkan = { workspace = true, optional = true }

log.workspace = true

[build-dependencies]
bindgen.workspace = true